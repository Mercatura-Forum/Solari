// Per-deployment settings, read by the app before its bundle loads. A deployment
// replaces this file; the source tree carries no contract id.
window.AUDIT_CID = 0;        // the firm contract a signed-in person without a firm lands on
window.AUDIT_REGISTRY = 0;   // the firm registry contract
// window.AUDIT_SINGLE_FIRM = true;   // one firm only: the registry is not consulted
